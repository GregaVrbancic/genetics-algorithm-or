export function configure(config) {
  //config.globalResources([]);
  config
    .globalResources([
      './elements/nav',
      './elements/foot',
      './elements/word-guess',
      './elements/travelling-salesman'
    ]);
}
